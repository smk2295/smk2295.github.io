<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://smk2295.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://smk2295.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-03T04:38:50+00:00</updated><id>https://smk2295.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Inference speed isn’t just a model-size problem</title><link href="https://smk2295.github.io/blog/2026/inference-acceleration/" rel="alternate" type="text/html" title="Inference speed isn’t just a model-size problem"/><published>2026-06-11T01:00:00+00:00</published><updated>2026-06-11T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2026/inference-acceleration</id><content type="html" xml:base="https://smk2295.github.io/blog/2026/inference-acceleration/"><![CDATA[<blockquote> <p>Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu, C. H., Gonzalez, J. E., Zhang, H., &amp; Stoica, I. (2023). <a href="https://arxiv.org/abs/2309.06180">Efficient Memory Management for Large Language Model Serving with PagedAttention</a>. SOSP 2023.</p> <p>Leviathan, Y., Kalman, M., &amp; Matias, Y. (2023). <a href="https://arxiv.org/abs/2211.17192">Fast Inference from Transformers via Speculative Decoding</a>. ICML 2023.</p> <p>Teerapittayanon, S., McDanel, B., &amp; Kung, H. T. (2017). <a href="https://arxiv.org/abs/1709.01686">BranchyNet: Fast Inference via Early Exiting from Deep Neural Networks</a>.</p> </blockquote> <p>None of these three papers prunes or quantizes a single weight, which is exactly why they’re worth reading together after two posts about doing precisely that. A model’s serving latency isn’t only a function of how many parameters it has — it’s also shaped by how decoding gets scheduled and how memory for the KV cache gets managed, and shrinking the model does nothing for either.</p> <h2 id="the-kv-cache-is-a-memory-management-problem-not-a-model-problem">The KV cache is a memory-management problem, not a model problem</h2> <p>Autoregressive generation caches key/value activations for every previous token so they don’t get recomputed at each step. For long sequences and many concurrent requests, that cache can end up larger than the model weights themselves, and its size keeps changing as generation proceeds. Standard serving systems allocate it contiguously per request — which fragments memory and wastes capacity in a way operating systems solved for process memory decades ago. PagedAttention just imports that solution: allocate KV-cache memory in fixed-size, non-contiguous blocks the way an OS pages virtual memory, and let the vLLM system built on top share blocks across requests wherever possible. Reported gain: 2–4x throughput at the same latency, with the model itself completely untouched.</p> <h2 id="the-sequential-bottleneck-doesnt-have-to-stay-sequential">The sequential bottleneck doesn’t have to stay sequential</h2> <p>Decoding one token at a time is inherently sequential — the next forward pass depends on the last token produced. Leviathan et al.’s observation is that a lot of tokens in a typical generation are, in hindsight, easy: predictable enough that a small, cheap draft model would have guessed the same token the large model eventually does. So let the draft model propose several tokens ahead, and have the large model verify the whole proposed run in one parallel pass, keeping whichever prefix matches. A rejection-sampling correction makes this exact, not approximate — the output distribution is identical to standard decoding, just faster. Reported: 2–3x speedup on T5-XXL, no retraining, no architecture change.</p> <h2 id="not-every-input-deserves-the-same-depth">Not every input deserves the same depth</h2> <p>BranchyNet is the odd one out — an architectural idea rather than a scheduling one. Attach classifiers at intermediate layers, and let an input exit through an early branch the moment its prediction there is confident enough, skipping whatever layers remain. Only the inputs that actually need the full network get it. It’s a bet that difficulty varies across inputs, and that spending the same fixed depth on all of them is wasted computation on the easy majority.</p> <table> <thead> <tr> <th>Method</th> <th>What it changes</th> </tr> </thead> <tbody> <tr> <td>PagedAttention</td> <td>How memory is laid out around a fixed model</td> </tr> <tr> <td>Speculative decoding</td> <td>How many forward passes are needed</td> </tr> <tr> <td>Early exit</td> <td>How much of the network an input actually traverses</td> </tr> </tbody> </table> <p>Stack these next to the pruning and quantization posts and the picture gets clearer: “make it faster” isn’t one problem with one lever. A perfectly pruned, perfectly quantized model can still crawl if it’s served on a bad memory layout, decoded one token at a time when it didn’t have to be, and run through every layer regardless of how easy the input was. Model size and inference speed are correlated. They are not the same thing.</p>]]></content><author><name></name></author><category term="research-notes"/><category term="model-compression"/><category term="inference-acceleration"/><category term="efficiency"/><summary type="html"><![CDATA[PagedAttention/vLLM, speculative decoding, and early-exit networks all speed up inference without shrinking a single parameter — three papers, one point.]]></summary></entry><entry><title type="html">Why joint compression beats doing it in sequence</title><link href="https://smk2295.github.io/blog/2026/joint-compression/" rel="alternate" type="text/html" title="Why joint compression beats doing it in sequence"/><published>2026-04-30T01:00:00+00:00</published><updated>2026-04-30T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2026/joint-compression</id><content type="html" xml:base="https://smk2295.github.io/blog/2026/joint-compression/"><![CDATA[<blockquote> <p>Wang, T., Wang, K., Cai, H., Lin, J., Liu, Z., &amp; Han, S. (2020). <a href="https://arxiv.org/abs/2006.08509">APQ: Joint Search for Network Architecture, Pruning and Quantization Policy</a>. CVPR 2020.</p> </blockquote> <p>The default way to combine pruning and quantization is to stage them: pick an architecture, decide a pruning policy for it, then decide a quantization policy for whatever survived pruning, fine-tuning in between. It’s a reasonable-sounding pipeline, and it has a structural flaw that has nothing to do with how carefully you tune each stage. A pruning stage that only ever sees the full-precision model has no way to know which channels will turn out to matter most once quantization error enters the picture — the two decisions are entangled in reality, but the pipeline resolves them as if they weren’t.</p> <h2 id="searching-all-three-at-once-cheaply">Searching all three at once, cheaply</h2> <p>APQ’s answer is to stop staging and treat architecture choice, per-layer pruning ratio, and per-layer bit-width as one combined space to search jointly. The obvious problem is cost — naively evaluating a single point in that space means training a candidate and measuring its accuracy, and the joint space dwarfs any one of the three sub-spaces on its own.</p> <p>Their way around it is a two-step trick with the training cost pushed as far to the side as possible. First, train one accuracy predictor for full-precision architectures using a “once-for-all” network — built so many sub-architectures can be evaluated without training each one separately, so the predictor itself is nearly free per candidate. Then transfer that predictor into a quantization-aware version, using a comparatively small number of <em>actual</em> quantized evaluations rather than building a large quantized-accuracy dataset from scratch.</p> <h2 id="what-thats-worth">What that’s worth</h2> <p>On ImageNet, at matched efficiency budgets:</p> <table> <thead> <tr> <th>Comparison</th> <th>Result</th> </tr> </thead> <tbody> <tr> <td>vs. staged search (architecture → pruning → quantization)</td> <td>+2.3% accuracy</td> </tr> <tr> <td>vs. MobileNetV2 + HAQ</td> <td>~2x lower latency, ~1.3x lower energy</td> </tr> <tr> <td>Search cost</td> <td>Substantially below prior joint NAS+compression methods</td> </tr> </tbody> </table> <p>None of this should read as “joint search discovered a clever trick.” A staged pipeline is leaving accuracy on the table <em>by construction</em> — the pruning decision literally cannot see the quantization policy that comes after it, so it has no way to account for the interaction between the two. A joint search, even an approximate one, at least has access to that interaction. The actual engineering work in the paper isn’t a smarter pruning or quantization rule; it’s making a combinatorially larger joint space cheap enough to search at all.</p> <p>That’s the general shape of the argument, beyond this one paper: once “how much to prune” and “how many bits to use” get treated as one decision under a shared budget instead of two independent knobs, compression turns into a constrained search problem, and most of the progress comes from making that search tractable rather than from a better criterion for either piece in isolation.</p>]]></content><author><name></name></author><category term="research-notes"/><category term="model-compression"/><category term="joint-optimization"/><category term="efficiency"/><summary type="html"><![CDATA[APQ (Wang et al., CVPR 2020) shows what changes when architecture, pruning, and quantization are searched for jointly instead of tuned one after another.]]></summary></entry><entry><title type="html">Quantization: fewer bits per weight, without losing the model</title><link href="https://smk2295.github.io/blog/2026/quantization-explained/" rel="alternate" type="text/html" title="Quantization: fewer bits per weight, without losing the model"/><published>2026-03-21T01:00:00+00:00</published><updated>2026-03-21T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2026/quantization-explained</id><content type="html" xml:base="https://smk2295.github.io/blog/2026/quantization-explained/"><![CDATA[<blockquote> <p>Dettmers, T., Lewis, M., Belkada, Y., &amp; Zettlemoyer, L. (2022). <a href="https://arxiv.org/abs/2208.07339">LLM.int8(): 8-bit Matrix Multiplication for Transformers at Scale</a>. NeurIPS 2022.</p> <p>Xiao, G., Lin, J., Seznec, M., Wu, H., Demouth, J., &amp; Han, S. (2023). <a href="https://arxiv.org/abs/2211.10438">SmoothQuant: Accurate and Efficient Post-Training Quantization for Large Language Models</a>. ICML 2023.</p> <p>Frantar, E., Ashkboos, S., Hoefler, T., &amp; Alistarh, D. (2023). <a href="https://arxiv.org/abs/2210.17323">GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers</a>. ICLR 2023.</p> </blockquote> <p>A basic quantizer maps a real value $x$ onto an integer grid using a scale $s$:</p> \[q = \text{round}(x / s), \qquad \hat x = s \cdot q\] <p>That’s the whole idea, and it barely works on large transformers. The reason isn’t random noise — it’s <strong>outliers</strong>. LLMs develop specific feature dimensions with activation magnitudes many times larger than everything around them, and if $s$ has to be wide enough to cover those, every ordinary value gets crushed into a handful of grid points. Three papers, three different ways of refusing to let outliers set the scale for everyone else.</p> <h2 id="give-the-outliers-their-own-lane">Give the outliers their own lane</h2> <p>LLM.int8() doesn’t force every value through one shared 8-bit scale. It quantizes the bulk of values with a vector-wise scheme and pulls the small number of outlier feature dimensions out entirely, computing just those in FP16 through a separate mixed-precision path — over 99.9% of values still end up in int8. The payoff: OPT-175B converts to Int8 and runs with no measured performance loss against the 16-bit original, at roughly half the memory.</p> <h2 id="move-the-difficulty-dont-hide-it">Move the difficulty, don’t hide it</h2> <p>SmoothQuant’s framing is blunt: weights are easy to quantize, activations aren’t. So instead of special-casing outliers at runtime, it rescales <em>before</em> quantization even happens — dividing the troublesome activation channels by a per-channel factor and multiplying the matching weight channels by the same factor. The matrix product is untouched, but the difficulty has moved from activations (hard) onto weights (easy). That’s enough to get full W8A8 — both weights <em>and</em> activations at 8 bits, not just weights — for roughly 1.56x speedup, about 2x less memory, and negligible accuracy loss, up to serving a 530B model on a single node.</p> <h2 id="stop-rounding-start-solving">Stop rounding, start solving</h2> <p>GPTQ throws out the fixed-rule approach entirely. It treats quantization as a per-layer optimization problem: given a small calibration set, solve directly for the quantized weights that minimize that layer’s output reconstruction error, using approximate second-order information to decide the order weights get quantized in and how to compensate for each one’s error. One-shot, no retraining — and it still compresses a 175B model to 3–4 bits per weight in about four GPU-hours, fitting it on a single GPU with roughly 3.25x measured speedup, remaining usable even pushed down to 2-bit.</p> <table> <thead> <tr> <th>Method</th> <th>What it changes</th> <th>Precision</th> <th>Headline result</th> </tr> </thead> <tbody> <tr> <td>LLM.int8()</td> <td>Outliers in FP16, rest in INT8</td> <td>W8 (mixed)</td> <td>175B model, no degradation, ~2x less memory</td> </tr> <tr> <td>SmoothQuant</td> <td>Rescales activations ↔ weights pre-quantization</td> <td>W8A8</td> <td>1.56x speedup, ~2x memory reduction</td> </tr> <tr> <td>GPTQ</td> <td>Solves per-layer reconstruction error</td> <td>W3–W4, down to 2-bit</td> <td>175B model on 1 GPU, ~3.25x speedup</td> </tr> </tbody> </table> <p>None of these three is “quantization” as a single fixed algorithm — each one found a specific structural weak point (which dimensions are outliers, which operand is actually hard to quantize, what a layer’s own reconstruction error looks like) and built the method to exploit it. That’s also roughly the order of difficulty: going from 8 bits down toward 2 needs progressively more of this kind of targeted machinery, because naive rounding simply stops working somewhere around there.</p>]]></content><author><name></name></author><category term="research-notes"/><category term="model-compression"/><category term="quantization"/><category term="efficiency"/><summary type="html"><![CDATA[LLM.int8(), SmoothQuant, and GPTQ — three concrete answers to the same problem, outlier activations that make naive low-bit quantization of LLMs fail.]]></summary></entry><entry><title type="html">Pruning: removing what a network doesn’t need</title><link href="https://smk2295.github.io/blog/2026/pruning-explained/" rel="alternate" type="text/html" title="Pruning: removing what a network doesn’t need"/><published>2026-02-04T01:00:00+00:00</published><updated>2026-02-04T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2026/pruning-explained</id><content type="html" xml:base="https://smk2295.github.io/blog/2026/pruning-explained/"><![CDATA[<blockquote> <p>Han, S., Pool, J., Tran, J., &amp; Dally, W. J. (2015). <a href="https://arxiv.org/abs/1506.02626">Learning both Weights and Connections for Efficient Neural Networks</a>. NeurIPS 2015.</p> <p>Frankle, J., &amp; Carbin, M. (2019). <a href="https://arxiv.org/abs/1803.03635">The Lottery Ticket Hypothesis: Finding Sparse, Trainable Neural Networks</a>. ICLR 2019.</p> </blockquote> <p>Most trained networks are carrying weight they don’t need. Pruning is the oldest compression idea there is: figure out which weights, channels, or heads contribute the least, and cut them.</p> <h2 id="the-recipe-and-what-it-actually-buys-you">The recipe, and what it actually buys you</h2> <p>Han et al.’s version is about as simple as it gets: train to convergence, drop every weight below a magnitude threshold, fine-tune what’s left to recover accuracy, and optionally repeat.</p> <table> <thead> <tr> <th>Network</th> <th style="text-align: right">Before</th> <th style="text-align: right">After</th> <th style="text-align: right">Reduction</th> </tr> </thead> <tbody> <tr> <td>AlexNet</td> <td style="text-align: right">61M params</td> <td style="text-align: right">6.7M params</td> <td style="text-align: right">9x</td> </tr> <tr> <td>VGG-16</td> <td style="text-align: right">138M params</td> <td style="text-align: right">10.3M params</td> <td style="text-align: right">13x</td> </tr> </tbody> </table> <p>Both with no reported accuracy loss — which sounds almost too easy, and the catch is in what kind of sparsity this produces. Magnitude is a per-weight criterion, so it doesn’t care about shape: the result is <strong>unstructured</strong> sparsity, a scattered pattern of zeros with no consistent structure. A 90%-sparse matrix like this has 90% fewer nonzero entries, but a standard dense-matmul kernel doesn’t know to skip them — it multiplies through the zeros exactly like everything else. Without sparse-aware kernels or hardware, the smaller checkpoint doesn’t translate into a faster one.</p> <p><strong>Structured</strong> pruning sidesteps this by removing whole channels, heads, or layers instead of scattered individual weights. The payoff is a genuinely smaller <em>dense</em> matrix that ordinary hardware already multiplies faster. The price is that you can’t push sparsity nearly as far before accuracy starts to break, since removal now happens in much coarser units.</p> <h2 id="the-subnetwork-that-was-there-all-along">The subnetwork that was there all along</h2> <p>Frankle and Carbin asked a sharper question about results like Han et al.’s: is the sparse subnetwork itself special, or would any sparse mask of the same size do just as well if you retrained it from scratch?</p> <p>Their test: take a pruned subnetwork, but instead of keeping its trained weights, reset every remaining weight back to its <em>original</em> pretraining initialization, then train from there. These “winning tickets” train to match or beat the full dense network’s accuracy, and do it <em>faster</em> — even at under 10–20% of the original parameter count. The same mask with a fresh <em>random</em> initialization usually can’t pull this off, which is the interesting part: it means both the sparse structure and the specific initial values pruning happened to land on are doing real work, not the mask shape alone.</p> <p>That result quietly changed what pruning is for. It stopped being just a way to shrink an already-trained network, and became a question about which sparse subnetworks are trainable in the first place — which is the question a whole line of later work on finding such subnetworks earlier in training is really trying to answer.</p> <p>Magnitude pruning, for all its simplicity, only ever looks at a weight’s own size, never its actual effect on the loss. The more careful criteria — tracing back to LeCun, Denker, and Solla’s <em>Optimal Brain Damage</em> from 1989 — use gradient or Hessian information instead, and the structured versions of that idea apply it to whole channels using calibration-data statistics rather than raw magnitude.</p>]]></content><author><name></name></author><category term="research-notes"/><category term="model-compression"/><category term="pruning"/><category term="efficiency"/><summary type="html"><![CDATA[From Optimal Brain Damage to the Lottery Ticket Hypothesis — what the pruning literature actually removes, and why unstructured sparsity doesn't automatically buy speed.]]></summary></entry><entry><title type="html">Is it the demonstrations, or the model?</title><link href="https://smk2295.github.io/blog/2025/uq-for-icl/" rel="alternate" type="text/html" title="Is it the demonstrations, or the model?"/><published>2025-11-14T01:00:00+00:00</published><updated>2025-11-14T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2025/uq-for-icl</id><content type="html" xml:base="https://smk2295.github.io/blog/2025/uq-for-icl/"><![CDATA[<blockquote> <p>Ling, C., Zhao, X., Zhang, X., Cheng, W., Liu, Y., Sun, Y., Oishi, M., Osaki, T., Matsuda, K., Ji, J., Bai, G., Zhao, L., &amp; Chen, H. (2024). <a href="https://arxiv.org/abs/2402.10189">Uncertainty Quantification for In-Context Learning of Large Language Models</a>. NAACL 2024.</p> </blockquote> <p>Every standard uncertainty tool for LLMs — predictive entropy, ensembling, MC dropout — was built assuming randomness comes from two places: the model’s parameters, and noise in generation. In-context learning sneaks in a third source none of those tools were designed to see: the prompt itself is a <em>choice</em>. Swap which demonstrations you pick, how many, or their order, and predictions shift on a task that hasn’t changed at all. That’s not the model being uncertain. That’s the prompt construction doing something.</p> <h2 id="splitting-the-blame">Splitting the blame</h2> <p>Ling et al. split an ICL prediction’s total predictive uncertainty into two pieces: an <strong>aleatoric</strong> term from randomness in which demonstrations got picked, and an <strong>epistemic</strong> term for whatever uncertainty is left once you’ve accounted for that. It’s the same law-of-total-variance move Kendall and Gal use for vision:</p> \[\text{Var}[\text{total}] = \underbrace{\text{Var}[\text{demonstration-set choice}]}_{\text{aleatoric}} + \underbrace{\mathbb{E}[\text{residual variance} \mid \text{fixed demo set}]}_{\text{epistemic}}\] <p>The estimator has to work without ever touching a gradient, since ICL has no training step to draw posterior samples from. So it resamples instead: draw many demonstration sets from the labeled pool for a task, run the model on each resulting prompt, and use how much the outputs spread across those sets as the aleatoric term, with within-prompt predictive entropy standing in for the epistemic term.</p> <h2 id="what-it-buys-you">What it buys you</h2> <p>Across multiple LLMs and classification/QA benchmarks, the decomposed estimate correlates with actual correctness and calibration better than undecomposed baselines like plain entropy on one fixed prompt. And the aleatoric piece behaves exactly as advertised: it tracks how much accuracy moves when you swap the demonstration set for a fixed model and task, confirming that a real chunk of what looks like “the model is uncertain” is actually “the prompt happened to be constructed this way.”</p> <p>The catch is <em>how</em> it gets there. Every input to the estimator is something the model output under a resampled prompt — never an internal activation. That makes it usable on any black-box model, but it also means uncertainty is only ever visible indirectly, through how much the final answer wobbles when you perturb the input, and getting a reliable estimate takes many forward passes per query.</p> <table> <thead> <tr> <th> </th> <th>This paper</th> <th>Reading the internals directly</th> </tr> </thead> <tbody> <tr> <td>Model access</td> <td>Black-box, outputs only</td> <td>White-box, activations</td> </tr> <tr> <td>Cost per query</td> <td>Many forward passes</td> <td>Can be closer to one</td> </tr> <tr> <td>What it measures</td> <td>Output movement under perturbation</td> <td>The task representation itself</td> </tr> </tbody> </table> <p>This is the closest prior work to what we ended up building, and reading it closely is what made the next question obvious: if function vectors give you a fairly direct handle on the model’s internal read of the task, could you get the same aleatoric/epistemic split <em>from that</em>, instead of from watching the output wobble under a hundred resampled prompts?</p>]]></content><author><name></name></author><category term="paper-notes"/><category term="uncertainty"/><category term="in-context-learning"/><summary type="html"><![CDATA[Ling, Zhao, Zhang et al. (NAACL 2024) decompose ICL's predictive uncertainty into a demonstration-selection component and a model component.]]></summary></entry><entry><title type="html">Two ways for a model to say “I don’t know”</title><link href="https://smk2295.github.io/blog/2025/aleatoric-vs-epistemic/" rel="alternate" type="text/html" title="Two ways for a model to say “I don’t know”"/><published>2025-09-27T01:00:00+00:00</published><updated>2025-09-27T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2025/aleatoric-vs-epistemic</id><content type="html" xml:base="https://smk2295.github.io/blog/2025/aleatoric-vs-epistemic/"><![CDATA[<blockquote> <p>Kendall, A., &amp; Gal, Y. (2017). <a href="https://arxiv.org/abs/1703.04977">What Uncertainties Do We Need in Bayesian Deep Learning for Computer Vision?</a> NeurIPS 2017.</p> </blockquote> <p>A model can be unsure for two completely different reasons, and it’s worth being pedantic about the difference because the fix is not the same. <strong>Aleatoric</strong> uncertainty is baked into the observation — sensor noise, occlusion, a genuinely ambiguous input — and no amount of extra training data touches it, because the world is what’s ambiguous, not the model. <strong>Epistemic</strong> uncertainty lives in the model’s parameters instead: it’s largest on inputs unlike anything it’s trained on, and it does shrink as relevant data accumulates. Kendall and Gal is the paper that made this split precise enough to actually build.</p> <h2 id="making-the-model-report-its-own-noise">Making the model report its own noise</h2> <p>For the aleatoric half, the network stops outputting a single point prediction $\hat y$ and instead outputs a prediction <em>and</em> a variance $\sigma^2(x)$ specific to that input. Plug that into a Gaussian negative log-likelihood and you get:</p> \[\mathcal{L} = \frac{1}{2\sigma^2(x)} \|y - \hat y\|^2 + \frac{1}{2}\log \sigma^2(x).\] <p>Nobody tells the network which inputs are noisy. The loss does it on its own: getting a “noisy” input wrong costs less once $\sigma^2(x)$ is allowed to grow, so the network learns to widen its own uncertainty exactly where it needs to, purely by minimizing this one objective.</p> <h2 id="making-the-model-report-its-own-ignorance">Making the model report its own ignorance</h2> <p>For epistemic uncertainty, the trick is almost absurdly cheap: leave dropout switched on at <em>test</em> time, run the same input through the network $T$ times, and look at how much the $T$ outputs disagree. Gal and Ghahramani had already shown this disagreement approximates sampling from a Bayesian posterior over the weights — so variance across those samples becomes a genuine (if approximate) measure of what the model doesn’t know.</p> <p>Put both pieces in one network and the total predictive variance decomposes cleanly:</p> \[\text{Var}[\text{total}] = \underbrace{\mathbb{E}[\sigma^2(x)]}_{\text{aleatoric}} + \underbrace{\text{Var}[\hat y]}_{\text{epistemic, across MC samples}}\] <p>Two numbers, estimated jointly, not two disconnected pipelines bolted together after the fact.</p> <h2 id="what-actually-shows-up-in-the-results">What actually shows up in the results</h2> <p>Tested on semantic segmentation (CamVid, Cityscapes) and depth regression (Make3D, NYUv2):</p> <table> <thead> <tr> <th>Finding</th> <th>Detail</th> </tr> </thead> <tbody> <tr> <td>Where aleatoric uncertainty is largest</td> <td>Object boundaries, small or distant objects — regions genuinely ambiguous from one image</td> </tr> <tr> <td>Where epistemic uncertainty is largest</td> <td>Inputs and classes underrepresented in training</td> </tr> <tr> <td>Combined vs. either alone</td> <td>Better calibrated, lower predictive loss</td> </tr> <tr> <td>Effect of more training data</td> <td>Aleatoric uncertainty comes to dominate; epistemic’s share shrinks — exactly what the definitions predict</td> </tr> </tbody> </table> <p>None of the machinery here — the heteroscedastic loss, MC dropout — transfers directly to a setting like in-context learning, where there’s no retraining loop to sample a weight posterior from. But the decomposition itself, and the finding that conflating the two produces a worse-calibrated model than separating them, is the part that later NLP and ICL uncertainty work keeps importing wholesale, even after throwing out everything else.</p>]]></content><author><name></name></author><category term="paper-notes"/><category term="uncertainty"/><category term="bayesian-deep-learning"/><summary type="html"><![CDATA[Kendall and Gal (NeurIPS 2017) give the standard recipe for modeling aleatoric and epistemic uncertainty jointly in a single network, on segmentation and depth regression.]]></summary></entry><entry><title type="html">Learning without a gradient step</title><link href="https://smk2295.github.io/blog/2025/icl-as-bayesian-inference/" rel="alternate" type="text/html" title="Learning without a gradient step"/><published>2025-08-09T01:00:00+00:00</published><updated>2025-08-09T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2025/icl-as-bayesian-inference</id><content type="html" xml:base="https://smk2295.github.io/blog/2025/icl-as-bayesian-inference/"><![CDATA[<blockquote> <p>Xie, S. M., Raghunathan, A., Liang, P., &amp; Ma, T. (2022). <a href="https://arxiv.org/abs/2111.02080">An Explanation of In-Context Learning as Implicit Bayesian Inference</a>. ICLR 2022.</p> </blockquote> <p>Here’s the thing about in-context learning that should bother you more than it usually does: nothing is being learned, in the sense we normally mean it. No gradient touches the weights between seeing the demonstrations and producing an answer. And yet performance keeps improving as you add more examples, which is exactly what learning is supposed to look like. Xie et al. wanted a pretraining story under which this isn’t a mystery but an inevitability.</p> <h2 id="a-pretraining-world-built-to-make-the-theory-provable">A pretraining world built to make the theory provable</h2> <p>They construct a synthetic pretraining distribution out of a mixture of Hidden Markov Models, one HMM per latent “document concept.” Generating a document means: sample a concept, then sample a long token sequence from that concept’s HMM, keeping the concept fixed for the whole document — a crude but workable stand-in for the fact that real documents keep a consistent topic or style from start to finish.</p> <p>Under this setup, they can actually prove something: a transformer trained to predict the next token, given enough pretraining data, ends up implicitly performing Bayesian inference over <em>which concept generated the document it’s currently reading</em>, using only the tokens seen so far. Nobody engineered that behavior in. It falls out of next-token prediction once the pretraining distribution has this document-concept structure.</p> <h2 id="why-that-explains-few-shot-prompting">Why that explains few-shot prompting</h2> <p>An ICL prompt is just a concatenation of demonstrations, and to a model trained this way, it looks like a partial document. Each demonstration is evidence about which concept is active; more demonstrations narrow the posterior over concepts further; and predicting under a sharper posterior is what, from the outside, reads as “the model learned the task.” All of it happens inside a single forward pass, over a belief the model holds about which concept applies — never over the weights themselves.</p> <p>The theory gets a synthetic testbed to match: <strong>GINC</strong>, built directly from an HMM mixture satisfying the paper’s own assumptions. On it, accuracy climbs with more demonstrations and holds up reasonably well under reordering — both exactly what the Bayesian account predicts. Real pretrained models (GPT-2, GPT-3) show similar demonstration-count trends on natural tasks, though the authors are careful to call this suggestive rather than proof that real LMs satisfy their formal setup.</p> <p>What I keep coming back to is the shift in what “uncertain” even means once you buy this framing. If the model is inferring a concept, then uncertainty is just a posterior that hasn’t concentrated — and now there’s a real question to ask: is the posterior spread out because the demonstrations themselves are genuinely ambiguous about which concept applies, or because the model’s read of clear evidence is bad? The first is a fact about the prompt. The second is a fact about the model. Nothing in the paper measures this split — it just hands you the vocabulary to ask for it.</p>]]></content><author><name></name></author><category term="paper-notes"/><category term="uncertainty"/><category term="in-context-learning"/><category term="theory"/><summary type="html"><![CDATA[Xie, Raghunathan, Liang, and Ma (ICLR 2022) give a formal Bayesian account of why in-context learning improves with more demonstrations without any gradient update.]]></summary></entry><entry><title type="html">A task, folded into a single vector</title><link href="https://smk2295.github.io/blog/2025/function-vectors/" rel="alternate" type="text/html" title="A task, folded into a single vector"/><published>2025-07-22T01:00:00+00:00</published><updated>2025-07-22T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2025/function-vectors</id><content type="html" xml:base="https://smk2295.github.io/blog/2025/function-vectors/"><![CDATA[<blockquote> <p>Todd, E., Li, M. L., Sharma, A. S., Mueller, A., Wallace, B. C., &amp; Bau, D. (2024). <a href="https://arxiv.org/abs/2310.15213">Function Vectors in Large Language Models</a>. ICLR 2024.</p> </blockquote> <p>Show a language model “hot → cold, big → small, fast → ___” and it fills in the blank correctly, no training involved. Somewhere in that single forward pass, the model has to be holding onto <em>which task the examples are asking for</em>. Todd et al. went looking for that thing, and found it’s smaller and more portable than you’d expect.</p> <h2 id="finding-the-vector">Finding the vector</h2> <p>The recipe has two steps. First, a causal-mediation analysis narrows down which attention heads actually matter for a given task — heads whose activations, when nudged, move the model’s output the most. Second, those heads’ activations get averaged across many few-shot prompts of the same task, at the final token position. Averaging washes out whatever is specific to any one example and leaves behind something that looks like a representation of the task itself: a <strong>function vector</strong>.</p> <p>This is tested across GPT-J, GPT-NeoX, Llama-2, and others, on tasks ranging from simple lexical mappings (antonym, synonym, translation) to more abstract relations.</p> <h2 id="the-test-that-matters">The test that matters</h2> <p>Finding a vector that correlates with a task is easy. The interesting claim is that it <em>causes</em> the task, and the experiment for that is clean: take the function vector, add it to a <strong>zero-shot</strong> prompt — no demonstrations, nothing — and see what happens. The model performs the task anyway, recovering most of the accuracy it would have gotten from the real few-shot examples, despite never seeing one.</p> <p>A few extra checks make the causal story harder to dismiss as coincidence:</p> <ul> <li>The same vector, extracted from one prompt template, still works when patched into a differently-worded prompt.</li> <li>Only a small, consistent handful of heads carry the effect — ablating exactly those heads kills the model’s ICL ability on the task; ablating an equal number of random heads doesn’t.</li> <li>Vectors for related tasks can be combined and still produce sensible, if not always fully interpretable, behavior — some hint of compositional structure in whatever space these vectors live in.</li> </ul> <p>What I find most useful about this result isn’t the mechanism by itself, it’s what it hands you afterward: “the task the model thinks it’s solving” stops being a vague notion and becomes a concrete object you can extract, perturb, and measure. That’s exactly the kind of object you need if you want to ask how <em>uncertain</em> a model is about what task it’s even doing — a question that’s much harder to ask when the only handle you have is the model’s final output.</p>]]></content><author><name></name></author><category term="paper-notes"/><category term="uncertainty"/><category term="in-context-learning"/><category term="interpretability"/><summary type="html"><![CDATA[Todd, Li, Sharma, Mueller, Wallace, and Bau (ICLR 2024) show that in-context learning induces a single, portable vector that causally encodes the task.]]></summary></entry><entry><title type="html">Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra</title><link href="https://smk2295.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/" rel="alternate" type="text/html" title="Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra"/><published>2024-05-14T00:00:00+00:00</published><updated>2024-05-14T00:00:00+00:00</updated><id>https://smk2295.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra</id><content type="html" xml:base="https://smk2295.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[We’re sharing updates across our Gemini family of models and a glimpse of Project Astra, our vision for the future of AI assistants.]]></summary></entry><entry><title type="html">Displaying External Posts on Your al-folio Blog</title><link href="https://smk2295.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog/" rel="alternate" type="text/html" title="Displaying External Posts on Your al-folio Blog"/><published>2022-04-23T23:20:09+00:00</published><updated>2022-04-23T23:20:09+00:00</updated><id>https://smk2295.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog</id><content type="html" xml:base="https://smk2295.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog/"><![CDATA[]]></content><author><name></name></author></entry></feed>