<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://smk2295.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://smk2295.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-30T11:03:34+00:00</updated><id>https://smk2295.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Inference speed isn’t just a model-size problem</title><link href="https://smk2295.github.io/blog/2026/inference-acceleration/" rel="alternate" type="text/html" title="Inference speed isn’t just a model-size problem"/><published>2026-06-11T01:00:00+00:00</published><updated>2026-06-11T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2026/inference-acceleration</id><content type="html" xml:base="https://smk2295.github.io/blog/2026/inference-acceleration/"><![CDATA[<p><strong>Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu, C. H., Gonzalez, J. E., Zhang, H., &amp; Stoica, I. (2023). <a href="https://arxiv.org/abs/2309.06180">Efficient Memory Management for Large Language Model Serving with PagedAttention</a>. SOSP 2023.</strong> <strong>Leviathan, Y., Kalman, M., &amp; Matias, Y. (2023). <a href="https://arxiv.org/abs/2211.17192">Fast Inference from Transformers via Speculative Decoding</a>. ICML 2023.</strong> <strong>Teerapittayanon, S., McDanel, B., &amp; Kung, H. T. (2017). <a href="https://arxiv.org/abs/1709.01686">BranchyNet: Fast Inference via Early Exiting from Deep Neural Networks</a>.</strong></p> <p>None of these three papers prunes or quantizes anything, and that’s the point of pairing them here: a transformer’s serving latency is also shaped by how autoregressive decoding is scheduled and how memory for the KV cache is managed — axes that shrinking the model doesn’t automatically address.</p> <h2 id="pagedattention--vllm-the-kv-cache-is-a-memory-management-problem">PagedAttention / vLLM: the KV Cache Is a Memory-Management Problem</h2> <p>During autoregressive generation, a transformer caches key/value activations for every previous token so they aren’t recomputed at each step. For long sequences and many concurrent requests, this cache can be <em>larger than the model weights themselves</em>, and its size changes dynamically as generation proceeds.</p> <p><strong>Diagnosis:</strong> standard serving systems allocate this cache contiguously per request, causing memory fragmentation and wasted capacity — the same problem operating systems solved for process memory decades ago.</p> <p><strong>Fix:</strong> PagedAttention allocates KV-cache memory in fixed-size, non-contiguous blocks, the way an OS pages virtual memory. The vLLM serving system built on top achieves near-zero cache waste and shares cache blocks across requests where possible.</p> <p><strong>Result:</strong> 2–4x throughput improvement over prior state-of-the-art serving systems at the same latency, with larger gains for longer sequences and larger models — entirely a scheduling and memory-layout change, with the model itself untouched.</p> <h2 id="speculative-decoding-make-the-sequential-bottleneck-parallel">Speculative Decoding: Make the Sequential Bottleneck Parallel</h2> <p>Autoregressive decoding is inherently sequential — one forward pass produces one token, and the next token’s forward pass depends on it.</p> <p><strong>Observation:</strong> many tokens in a generated sequence are, in retrospect, “easy” — predictable enough that a much smaller, cheaper draft model would have guessed the same token as the large target model.</p> <p><strong>Method:</strong> the small draft model proposes several tokens ahead; the large model verifies the entire proposed continuation in a single parallel forward pass, accepting whichever prefix matches what it would have produced on its own. A rejection-sampling correction guarantees the output distribution is <em>exactly</em> the same as standard decoding — not an approximation.</p> <p><strong>Result:</strong> 2–3x speedup on T5-XXL, with no retraining or architecture change to either model, and no change to the output distribution.</p> <h2 id="early-exit--branchynet-not-every-input-needs-the-full-network">Early Exit / BranchyNet: Not Every Input Needs the Full Network</h2> <p>Side-branch classifiers are attached at intermediate layers. An input exits through an early branch once its prediction there is confident enough, skipping the remaining layers entirely; only inputs that fail the confidence check proceed deeper.</p> <p>Unlike the two methods above, this is an <strong>architectural</strong> premise, not a scheduling or parallelism one: it assumes inputs vary in difficulty, and spends compute proportionally rather than running every input through the same fixed depth.</p> <h2 id="why-grouping-these-three-together-matters">Why Grouping These Three Together Matters</h2> <table> <thead> <tr> <th>Method</th> <th>What it changes</th> <th>Touches model weights?</th> </tr> </thead> <tbody> <tr> <td>PagedAttention</td> <td>How memory is laid out around a fixed model</td> <td>No</td> </tr> <tr> <td>Speculative decoding</td> <td>How many forward passes are needed</td> <td>No</td> </tr> <tr> <td>Early exit</td> <td>How much of the network an input traverses</td> <td>No</td> </tr> </tbody> </table> <p>All three are legitimate “make it faster” techniques that never touch a single weight value. A serving system that only invests in a smaller model — via pruning or quantization — while ignoring scheduling, memory layout, and input-dependent compute is leaving comparable or larger gains unaddressed.</p> <h2 id="takeaways">Takeaways</h2> <ol> <li>Model size and inference speed are correlated, not identical — several of the largest available speedups live entirely outside the model itself.</li> <li>KV-cache management, decoding parallelism, and input-dependent depth are three genuinely separate bottlenecks, not three names for the same one.</li> <li>A well-compressed model served on a bad memory/scheduling stack can still be slow — compression and serving-system design are complementary, not substitutes.</li> </ol>]]></content><author><name></name></author><category term="research-notes"/><category term="model-compression"/><category term="inference-acceleration"/><category term="efficiency"/><summary type="html"><![CDATA[PagedAttention/vLLM, speculative decoding, and early-exit networks all speed up inference without shrinking a single parameter — three papers, one point.]]></summary></entry><entry><title type="html">Why joint compression beats doing it in sequence</title><link href="https://smk2295.github.io/blog/2026/joint-compression/" rel="alternate" type="text/html" title="Why joint compression beats doing it in sequence"/><published>2026-04-30T01:00:00+00:00</published><updated>2026-04-30T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2026/joint-compression</id><content type="html" xml:base="https://smk2295.github.io/blog/2026/joint-compression/"><![CDATA[<p><strong>Wang, T., Wang, K., Cai, H., Lin, J., Liu, Z., &amp; Han, S. (2020). <a href="https://arxiv.org/abs/2006.08509">APQ: Joint Search for Network Architecture, Pruning and Quantization Policy</a>. CVPR 2020.</strong></p> <h2 id="the-problem-with-sequential-pipelines">The Problem With Sequential Pipelines</h2> <p>The default way to combine compression techniques is to apply them in stages: design or select an architecture, then decide a pruning policy for it, then decide a quantization policy for what’s left, fine-tuning between stages.</p> <p>Each stage makes its decision based only on the model as it exists <em>at that point</em>, with no knowledge of what the next stage will do to it. A channel judged “important enough to keep” by a pruning stage that only sees the full-precision model isn’t necessarily the channel most worth keeping once quantization error is also accounted for — the two decisions are entangled, but a staged pipeline resolves them independently anyway.</p> <h2 id="apqs-approach-search-the-joint-space-but-make-it-cheap">APQ’s Approach: Search the Joint Space, But Make It Cheap</h2> <p>APQ treats architecture choice, per-layer pruning ratio, and per-layer bit-width as <strong>one combined search space</strong> and searches over it jointly rather than staging the three decisions.</p> <p>The obstacle is cost: naively, evaluating one point in this joint space means training or fine-tuning a candidate configuration and measuring its accuracy, and the joint space is far larger than any one of the three sub-spaces alone. APQ’s contribution is a way around that cost:</p> <ol> <li>Train a single accuracy predictor for full-precision candidate architectures using a “once-for-all” network — built so many sub-architectures can be evaluated without separately training each one. This predictor itself requires no extra training cost per candidate.</li> <li>Transfer that full-precision predictor into a <strong>quantization-aware predictor</strong>, using a comparatively small number of actual quantized (architecture, pruning, quantization) evaluations, rather than collecting a large quantized-accuracy dataset from scratch.</li> </ol> <h2 id="reported-results">Reported Results</h2> <p>On ImageNet, at a matched efficiency budget:</p> <table> <thead> <tr> <th>Comparison</th> <th>Result</th> </tr> </thead> <tbody> <tr> <td>vs. staged search (architecture → pruning → quantization separately)</td> <td>+2.3% accuracy</td> </tr> <tr> <td>vs. MobileNetV2 + HAQ (strong prior quantization-search method)</td> <td>~2x lower latency, ~1.3x lower energy</td> </tr> <tr> <td>Search/data-collection cost</td> <td>Substantially lower than prior joint NAS+compression approaches</td> </tr> </tbody> </table> <h2 id="why-the-accuracy-gain-isnt-surprising-given-the-setup">Why the Accuracy Gain Isn’t Surprising, Given the Setup</h2> <p>The point isn’t that joint search finds some clever trick unavailable to staged pipelines — it’s that a staged pipeline is leaving information on the table <strong>by construction</strong>. The pruning decision in a staged pipeline literally cannot see the quantization policy applied afterward, so it can’t account for how the two interact. A joint search, even an approximate one, has access to that interaction.</p> <p>The real engineering content of the paper is elsewhere: a joint search space is combinatorially larger, and most of APQ’s method is about making evaluation of that larger space cheap enough to be tractable — not about a fundamentally different compression technique.</p> <h2 id="takeaways">Takeaways</h2> <ol> <li>Staged compression pipelines are structurally blind to interactions between stages — not just suboptimal by chance.</li> <li>Framing “how much to prune” and “how many bits to use” as one joint decision under a shared budget turns compression into a constrained combinatorial search problem.</li> <li>Progress in this direction usually comes from making the joint search <em>cheap enough to run</em>, not from a better pruning or quantization criterion in isolation.</li> </ol>]]></content><author><name></name></author><category term="research-notes"/><category term="model-compression"/><category term="joint-optimization"/><category term="efficiency"/><summary type="html"><![CDATA[APQ (Wang et al., CVPR 2020) shows what changes when architecture, pruning, and quantization are searched for jointly instead of tuned one after another.]]></summary></entry><entry><title type="html">Quantization: fewer bits per weight, without losing the model</title><link href="https://smk2295.github.io/blog/2026/quantization-explained/" rel="alternate" type="text/html" title="Quantization: fewer bits per weight, without losing the model"/><published>2026-03-21T01:00:00+00:00</published><updated>2026-03-21T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2026/quantization-explained</id><content type="html" xml:base="https://smk2295.github.io/blog/2026/quantization-explained/"><![CDATA[<p><strong>Dettmers, T., Lewis, M., Belkada, Y., &amp; Zettlemoyer, L. (2022). <a href="https://arxiv.org/abs/2208.07339">LLM.int8(): 8-bit Matrix Multiplication for Transformers at Scale</a>. NeurIPS 2022.</strong> <strong>Xiao, G., Lin, J., Seznec, M., Wu, H., Demouth, J., &amp; Han, S. (2023). <a href="https://arxiv.org/abs/2211.10438">SmoothQuant: Accurate and Efficient Post-Training Quantization for Large Language Models</a>. ICML 2023.</strong> <strong>Frantar, E., Ashkboos, S., Hoefler, T., &amp; Alistarh, D. (2023). <a href="https://arxiv.org/abs/2210.17323">GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers</a>. ICLR 2023.</strong></p> <h2 id="the-shared-problem-outliers">The Shared Problem: Outliers</h2> <p>Naively rounding a large transformer’s 16-bit weights and activations to 8 or 4 bits produces unacceptable accuracy loss, and the reason isn’t random noise — it’s <strong>outliers</strong>. Large transformers develop specific feature dimensions with activation magnitudes many times larger than the rest of the tensor. A quantization scale sized to cover those outliers leaves almost no resolution for the “normal” values, which is where most of the model’s useful signal lives.</p> <p>A basic uniform quantizer maps a real value $x$ to an integer level using a scale $s$:</p> \[q = \text{round}(x / s), \qquad \hat x = s \cdot q\] <p>If $s$ has to be large enough to cover a rare outlier, every ordinary value gets rounded far more coarsely than it needs to be. Each of the three papers below is a different answer to this exact tension.</p> <h2 id="llmint8-keep-the-outliers-in-higher-precision">LLM.int8(): Keep the Outliers in Higher Precision</h2> <p>Rather than force every value through one 8-bit scale, LLM.int8() uses vector-wise quantization for the bulk of values and identifies the small number of outlier feature dimensions, computing <em>those specific dimensions</em> in 16-bit floating point via a separate mixed-precision path — while quantizing more than 99.9% of values to int8.</p> <p><strong>Result:</strong> a 175B-parameter model (OPT-175B) converts to Int8 and runs with no performance degradation relative to the 16-bit original, roughly halving the memory footprint and making models of that scale runnable on far more modest hardware.</p> <h2 id="smoothquant-move-the-difficulty-instead-of-hiding-it">SmoothQuant: Move the Difficulty Instead of Hiding It</h2> <p>The framing: “weights are easy to quantize, activations are not.” Instead of special-casing outlier dimensions at runtime, SmoothQuant applies a mathematically equivalent rescaling <em>before</em> quantization — dividing problematic activation channels by a per-channel smoothing factor and multiplying the corresponding weight channels by the same factor. The matrix product is unchanged, but quantization difficulty shifts from the hard-to-quantize activations onto the easy-to-quantize weights.</p> <p>This enables full <strong>W8A8</strong> (8-bit weights, 8-bit activations) — both operands low-precision, not just weights.</p> <p><strong>Result:</strong> up to 1.56x inference speedup and roughly 2x memory reduction with negligible accuracy loss, including serving a 530B-parameter model on a single compute node.</p> <h2 id="gptq-solve-for-the-weights-directly-and-go-lower">GPTQ: Solve for the Weights Directly, and Go Lower</h2> <p>Rather than a fixed rounding rule, GPTQ formulates quantization as a layer-by-layer optimization problem: given a small calibration dataset, solve for the quantized weight values that minimize the layer’s output reconstruction error, using approximate second-order (Hessian) information to decide the order and adjustment as each weight is quantized. It’s one-shot and post-training — no retraining loop.</p> <p><strong>Result:</strong> compresses a 175B-parameter model to 3–4 bits per weight in about four GPU-hours while preserving accuracy, fits it onto a single GPU for inference, and reports roughly 3.25x measured latency speedup on A100 GPUs — remaining reasonably accurate even pushed to 2-bit or ternary weights.</p> <h2 id="comparing-the-three">Comparing the Three</h2> <table> <thead> <tr> <th>Method</th> <th>What it changes</th> <th>Precision achieved</th> <th>Headline result</th> </tr> </thead> <tbody> <tr> <td>LLM.int8()</td> <td>Keeps outlier dims in FP16, rest in INT8</td> <td>W8 (mixed)</td> <td>175B model, no degradation, ~2x less memory</td> </tr> <tr> <td>SmoothQuant</td> <td>Rescales activations↔weights before quantizing</td> <td>W8A8</td> <td>1.56x speedup, ~2x memory reduction</td> </tr> <tr> <td>GPTQ</td> <td>Solves per-layer reconstruction error directly</td> <td>W3–W4 (down to 2-bit)</td> <td>175B model on 1 GPU, ~3.25x speedup</td> </tr> </tbody> </table> <h2 id="takeaways">Takeaways</h2> <ol> <li>None of these three treats “quantization” as one fixed algorithm — each identifies a specific structural property (outlier channels, easy-vs-hard-to-quantize operands, per-layer reconstruction error) and designs around it.</li> <li>Handling outliers well is the difference between quantization that works on LLMs and quantization that collapses accuracy.</li> <li>Going from 8-bit to 4-bit or lower needs progressively more careful machinery (GPTQ’s per-layer optimization) — naive rounding doesn’t scale down gracefully on its own.</li> </ol>]]></content><author><name></name></author><category term="research-notes"/><category term="model-compression"/><category term="quantization"/><category term="efficiency"/><summary type="html"><![CDATA[LLM.int8(), SmoothQuant, and GPTQ — three concrete answers to the same problem, outlier activations that make naive low-bit quantization of LLMs fail.]]></summary></entry><entry><title type="html">Pruning: removing what a network doesn’t need</title><link href="https://smk2295.github.io/blog/2026/pruning-explained/" rel="alternate" type="text/html" title="Pruning: removing what a network doesn’t need"/><published>2026-02-04T01:00:00+00:00</published><updated>2026-02-04T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2026/pruning-explained</id><content type="html" xml:base="https://smk2295.github.io/blog/2026/pruning-explained/"><![CDATA[<p><strong>Han, S., Pool, J., Tran, J., &amp; Dally, W. J. (2015). <a href="https://arxiv.org/abs/1506.02626">Learning both Weights and Connections for Efficient Neural Networks</a>. NeurIPS 2015.</strong> <strong>Frankle, J., &amp; Carbin, M. (2019). <a href="https://arxiv.org/abs/1803.03635">The Lottery Ticket Hypothesis: Finding Sparse, Trainable Neural Networks</a>. ICLR 2019.</strong></p> <h2 id="the-basic-method-han-et-al-2015">The Basic Method (Han et al., 2015)</h2> <ol> <li>Train a dense network to convergence.</li> <li>Remove all weights below a magnitude threshold.</li> <li>Retrain (fine-tune) the remaining sparse network to recover accuracy.</li> <li>Optionally iterate the prune-then-retrain cycle.</li> </ol> <table> <thead> <tr> <th>Network</th> <th style="text-align: right">Before</th> <th style="text-align: right">After</th> <th style="text-align: right">Reduction</th> <th>Accuracy loss</th> </tr> </thead> <tbody> <tr> <td>AlexNet</td> <td style="text-align: right">61M params</td> <td style="text-align: right">6.7M params</td> <td style="text-align: right">9x</td> <td>none reported</td> </tr> <tr> <td>VGG-16</td> <td style="text-align: right">138M params</td> <td style="text-align: right">10.3M params</td> <td style="text-align: right">13x</td> <td>none reported</td> </tr> </tbody> </table> <p>The criterion is magnitude, applied to individual weights — it says nothing about the <em>shape</em> of what’s removed, which is why it produces <strong>unstructured</strong> sparsity: a scattered pattern of zeros with no consistent shape.</p> <h2 id="why-unstructured-sparsity-doesnt-automatically-mean-faster">Why Unstructured Sparsity Doesn’t Automatically Mean Faster</h2> <p>A 90%-sparse unstructured weight matrix has 90% fewer nonzero entries, but a dense-matrix-multiply kernel on standard hardware doesn’t know to skip the zeros — it multiplies through them like any other value. Realizing an actual speedup requires sparse-matrix kernels or hardware with native sparse support, neither of which is universal.</p> <p>This is why <strong>structured</strong> pruning — removing entire channels, attention heads, or layers rather than individual weights — is more directly useful for latency:</p> <ul> <li>The result is a smaller <em>dense</em> matrix that ordinary kernels already multiply faster.</li> <li>The cost: less aggressive removal is tolerated before accuracy degrades, since removal happens in coarser units.</li> </ul> <h2 id="the-lottery-ticket-hypothesis-frankle--carbin-2019">The Lottery Ticket Hypothesis (Frankle &amp; Carbin, 2019)</h2> <p>A follow-up question about pruning results like Han et al.’s: is the sparse subnetwork itself special, or does any sparse mask of the same size work equally well if retrained from scratch?</p> <p><strong>The experiment.</strong> Take a pruned subnetwork, but instead of keeping its trained weights, reset each remaining weight to its <em>original pretraining initialization value</em>, then train from there.</p> <p><strong>The result.</strong> These “winning tickets” — sparse subnetworks paired with their original initialization —</p> <ul> <li>train to match or exceed the accuracy of the full dense network,</li> <li>do so <em>faster</em> than the dense network,</li> <li>even at sparsity levels below 10–20% of the original parameter count.</li> </ul> <p>Critically, the same sparse mask with freshly <em>random</em> re-initialization usually fails to match this performance — implying both the structure <em>and</em> the specific initial values found by pruning are doing real work, not the mask shape alone.</p> <p>This reframed pruning from “a way to compress an already-trained network” into a question about which sparse subnetworks are trainable in the first place, and motivated later work on identifying such subnetworks earlier in (or even before) training.</p> <h2 id="beyond-plain-magnitude">Beyond Plain Magnitude</h2> <p>Magnitude pruning is a strong, simple baseline, but it only looks at a weight’s own value, not its effect on the loss. Criteria building on classic second-order pruning ideas (going back to LeCun, Denker, and Solla’s <em>Optimal Brain Damage</em>, NeurIPS 1989) estimate each weight’s contribution to the loss via gradient or Hessian information; structured variants apply the same idea to whole channels or heads using calibration-data activation statistics instead of magnitude.</p> <h2 id="takeaways">Takeaways</h2> <ol> <li>Magnitude pruning is simple and works well, but it produces unstructured sparsity that most hardware can’t turn into real speed without special kernels.</li> <li>Structured pruning trades some achievable sparsity for guaranteed latency gains on ordinary hardware.</li> <li>The Lottery Ticket Hypothesis shows that a good sparse mask paired with its original initialization can match — and train faster than — the full network, which reframes pruning as a question about trainable subnetworks, not just post-hoc compression.</li> </ol>]]></content><author><name></name></author><category term="research-notes"/><category term="model-compression"/><category term="pruning"/><category term="efficiency"/><summary type="html"><![CDATA[From Optimal Brain Damage to the Lottery Ticket Hypothesis — what the pruning literature actually removes, and why unstructured sparsity doesn't automatically buy speed.]]></summary></entry><entry><title type="html">Paper notes — Uncertainty Quantification for In-Context Learning of LLMs</title><link href="https://smk2295.github.io/blog/2025/uq-for-icl/" rel="alternate" type="text/html" title="Paper notes — Uncertainty Quantification for In-Context Learning of LLMs"/><published>2025-11-14T01:00:00+00:00</published><updated>2025-11-14T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2025/uq-for-icl</id><content type="html" xml:base="https://smk2295.github.io/blog/2025/uq-for-icl/"><![CDATA[<p><strong>Ling, C., Zhao, X., Zhang, X., Cheng, W., Liu, Y., Sun, Y., Oishi, M., Osaki, T., Matsuda, K., Ji, J., Bai, G., Zhao, L., &amp; Chen, H. (2024). <a href="https://arxiv.org/abs/2402.10189">Uncertainty Quantification for In-Context Learning of Large Language Models</a>. NAACL 2024 (main conference).</strong></p> <h2 id="why-standard-uq-tools-dont-fit-icl">Why Standard UQ Tools Don’t Fit ICL</h2> <p>Standard uncertainty-quantification tools for LLMs — predictive entropy, ensembling, MC-dropout-style methods — were designed around two sources of randomness: the model’s parameters and randomness in generation. In-context learning introduces a third source none of those tools were built to isolate: <strong>the prompt itself is a choice</strong>. Which demonstrations are picked, how many, and in what order can shift predictions on a fixed underlying task — a property of the prompt construction, not of the model or of label noise.</p> <h2 id="the-decomposition">The Decomposition</h2> <p>The paper splits an ICL prediction’s <em>total predictive uncertainty</em> into two named components:</p> <ul> <li><strong>Aleatoric uncertainty</strong> — attributed to randomness in which demonstrations were selected. Estimated by resampling many different demonstration sets (and/or orderings) for the same task and measuring how much the predictive distribution varies across them.</li> <li><strong>Epistemic (“configuration”) uncertainty</strong> — the residual uncertainty that remains even after accounting for demonstration selection, attributed to the model’s own limitations.</li> </ul> <p>Formally, this follows the same law-of-total-variance decomposition Kendall and Gal use for vision uncertainty:</p> \[\text{Var}[\text{total}] = \underbrace{\text{Var}[\text{demonstration-set choice}]}_{\text{aleatoric}} + \underbrace{\mathbb{E}[\text{residual variance} \mid \text{fixed demo set}]}_{\text{epistemic}}\] <h2 id="estimating-it-without-retraining">Estimating It Without Retraining</h2> <p>ICL has no gradient step to get posterior samples from, so the estimator works by <strong>resampling and re-inference</strong>:</p> <ol> <li>Draw many demonstration sets from the available pool of labeled examples for a task.</li> <li>Run the model on each resulting prompt.</li> <li>Use the spread of the resulting output distributions as the aleatoric term.</li> <li>Use the within-prompt predictive entropy (or similar) as the epistemic term.</li> </ol> <h2 id="evaluation">Evaluation</h2> <p>Tested across multiple LLMs and classification/QA-style ICL benchmarks, against baselines that don’t separate the two sources (e.g., plain predictive entropy on a single fixed prompt):</p> <ul> <li>The decomposed estimate correlates better with actual prediction correctness/calibration than the undecomposed baselines.</li> <li>The aleatoric component specifically tracks how much accuracy varies when the demonstration set is swapped for a fixed model and task — confirming that a meaningful share of ICL’s apparent uncertainty is attributable to prompt construction, not the model.</li> </ul> <h2 id="where-this-sits-relative-to-function-vector-approaches">Where This Sits Relative to Function-Vector Approaches</h2> <p>The estimator’s inputs are entirely the model’s <em>outputs</em> under many resampled prompts — it never inspects internal activations.</p> <table> <thead> <tr> <th> </th> <th>This paper</th> <th>Internal-representation approaches</th> </tr> </thead> <tbody> <tr> <td>Model access needed</td> <td>Black-box (outputs only)</td> <td>White-box (activations)</td> </tr> <tr> <td>Cost per query</td> <td>Many forward passes</td> <td>Can be closer to one pass</td> </tr> <tr> <td>What it measures</td> <td>How much the output moves under input perturbation</td> <td>The model’s internal task representation directly</td> </tr> </tbody> </table> <h2 id="takeaways">Takeaways</h2> <ol> <li>A meaningful share of what looks like ICL “uncertainty” is really sensitivity to which demonstrations happened to be picked — not a property of the model alone.</li> <li>Being model-agnostic is a real advantage, but it comes at the cost of needing many forward passes and only ever seeing uncertainty indirectly, through output perturbation rather than internal representation.</li> <li>This is the closest prior baseline for uncertainty in ICL, and the natural question it leaves open is whether the same decomposition can be estimated more directly and cheaply from the model’s internals — which is where function vectors come back in.</li> </ol>]]></content><author><name></name></author><category term="paper-notes"/><category term="uncertainty"/><category term="in-context-learning"/><summary type="html"><![CDATA[Ling, Zhao, Zhang et al. (NAACL 2024) decompose ICL's predictive uncertainty into a demonstration-selection component and a model component.]]></summary></entry><entry><title type="html">Paper notes — What Uncertainties Do We Need in Bayesian Deep Learning?</title><link href="https://smk2295.github.io/blog/2025/aleatoric-vs-epistemic/" rel="alternate" type="text/html" title="Paper notes — What Uncertainties Do We Need in Bayesian Deep Learning?"/><published>2025-09-27T01:00:00+00:00</published><updated>2025-09-27T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2025/aleatoric-vs-epistemic</id><content type="html" xml:base="https://smk2295.github.io/blog/2025/aleatoric-vs-epistemic/"><![CDATA[<p><strong>Kendall, A., &amp; Gal, Y. (2017). <a href="https://arxiv.org/abs/1703.04977">What Uncertainties Do We Need in Bayesian Deep Learning for Computer Vision?</a> NeurIPS 2017.</strong></p> <h2 id="two-kinds-of-uncertain">Two Kinds of “Uncertain”</h2> <ul> <li><strong>Aleatoric</strong> uncertainty is inherent to the observation itself — sensor noise, occlusion, genuinely ambiguous input. It does not shrink with more training data, because the ambiguity isn’t in the model.</li> <li><strong>Epistemic</strong> uncertainty lives in the model’s parameters. It does shrink with more relevant training data, and is largest on inputs unlike anything in the training distribution.</li> </ul> <h2 id="modeling-aleatoric-uncertainty-a-heteroscedastic-loss">Modeling Aleatoric Uncertainty: A Heteroscedastic Loss</h2> <p>Instead of a network that outputs only a point prediction $\hat y$, the network outputs both a prediction and a predicted variance $\sigma^2(x)$ for that specific input. For regression, the negative log-likelihood under a Gaussian with input-dependent variance is</p> \[\mathcal{L} = \frac{1}{2\sigma^2(x)} \|y - \hat y\|^2 + \frac{1}{2}\log \sigma^2(x).\] <p>This loss naturally penalizes overconfident wrong answers less on inputs the network flags as noisy (large $\sigma^2$), while forcing confident correctness on inputs it doesn’t. No separate supervision for “what counts as noisy” is needed — the variance head is trained end-to-end, purely from this loss.</p> <h2 id="modeling-epistemic-uncertainty-monte-carlo-dropout">Modeling Epistemic Uncertainty: Monte Carlo Dropout</h2> <p>The network keeps dropout <strong>active at test time</strong> (not just during training) and is run $T$ times on the same input, producing $T$ slightly different outputs from the different dropout masks. Gal and Ghahramani’s earlier result — which this paper builds on — shows this is a valid approximate sample from a Bayesian posterior over network weights. The variance across the $T$ outputs is the epistemic uncertainty estimate.</p> <h2 id="combining-both">Combining Both</h2> <p>The full model has both the heteroscedastic variance head and MC-dropout sampling at test time. Total predictive variance is derived as:</p> \[\text{Var}[\text{total}] = \underbrace{\mathbb{E}[\sigma^2(x)]}_{\text{aleatoric}} + \underbrace{\text{Var}[\hat y]}_{\text{epistemic, across MC samples}}\] <p>The two uncertainty types add — they are estimated jointly in one model, not by two disconnected pipelines.</p> <h2 id="tasks-and-results">Tasks and Results</h2> <p>Evaluated on semantic segmentation (CamVid, Cityscapes) and monocular depth regression (Make3D, NYUv2):</p> <table> <thead> <tr> <th>Finding</th> <th>Detail</th> </tr> </thead> <tbody> <tr> <td>Aleatoric uncertainty location</td> <td>Largest at object boundaries and on small/distant objects — regions genuinely ambiguous from a single image</td> </tr> <tr> <td>Epistemic uncertainty location</td> <td>Largest on inputs and object classes underrepresented in training</td> </tr> <tr> <td>Combined vs. either alone</td> <td>Better-calibrated, lower predictive loss</td> </tr> <tr> <td>Effect of scale</td> <td>As training data grows, aleatoric uncertainty dominates and epistemic uncertainty’s relative share shrinks — consistent with the definitions</td> </tr> </tbody> </table> <h2 id="takeaways">Takeaways</h2> <ol> <li>Conflating the two uncertainty types produces worse-calibrated models than modeling them jointly, even though modeling both costs little extra machinery.</li> <li>The heteroscedastic loss and MC-dropout mechanics here are vision-specific, but the <em>decomposition itself</em> — and the finding that it matters — is the template later NLP and in-context-learning uncertainty work explicitly imports.</li> <li>Neither mechanism transfers directly to autoregressive generation or to a no-retraining setting like ICL, which is exactly the gap that later work (including the paper this reading list supports) has to fill with a different estimator.</li> </ol>]]></content><author><name></name></author><category term="paper-notes"/><category term="uncertainty"/><category term="bayesian-deep-learning"/><summary type="html"><![CDATA[Kendall and Gal (NeurIPS 2017) give the standard recipe for modeling aleatoric and epistemic uncertainty jointly in a single network, on segmentation and depth regression.]]></summary></entry><entry><title type="html">Paper notes — In-Context Learning as Implicit Bayesian Inference</title><link href="https://smk2295.github.io/blog/2025/icl-as-bayesian-inference/" rel="alternate" type="text/html" title="Paper notes — In-Context Learning as Implicit Bayesian Inference"/><published>2025-08-09T01:00:00+00:00</published><updated>2025-08-09T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2025/icl-as-bayesian-inference</id><content type="html" xml:base="https://smk2295.github.io/blog/2025/icl-as-bayesian-inference/"><![CDATA[<p><strong>Xie, S. M., Raghunathan, A., Liang, P., &amp; Ma, T. (2022). <a href="https://arxiv.org/abs/2111.02080">An Explanation of In-Context Learning as Implicit Bayesian Inference</a>. ICLR 2022.</strong></p> <h2 id="the-puzzle">The Puzzle</h2> <p>In-context learning has no obvious mechanism: no gradient step separates seeing the demonstrations from producing the answer. The paper asks under what pretraining conditions this behavior should be <em>expected</em> to emerge from a plain next-token-prediction objective, rather than treated as an unexplained empirical curiosity.</p> <h2 id="a-synthetic-pretraining-distribution">A Synthetic Pretraining Distribution</h2> <p>The authors construct a pretraining distribution generated by a mixture of Hidden Markov Models (HMMs), where each HMM instance corresponds to a distinct latent <strong>document concept</strong>:</p> <ul> <li>A document is generated by first sampling a concept, then sampling a long token sequence from that concept’s HMM.</li> <li>Within one document, the concept stays fixed — meant to mimic how real documents keep a consistent topic or style throughout.</li> </ul> <p>They prove that a Transformer trained to predict the next token under this generative process is, in the large-pretraining-data limit, implicitly performing <strong>Bayesian inference over which latent concept generated the current document</strong>, conditioned only on the tokens observed so far.</p> <h2 id="why-this-predicts-in-context-learning">Why This Predicts In-Context Learning</h2> <p>At test time, an ICL prompt — a concatenation of demonstrations — looks to the pretrained model like a partial document:</p> <ul> <li>Each demonstration is additional evidence about which latent concept is active.</li> <li>Conditioning on more demonstrations narrows the posterior over concepts.</li> <li>The model’s next-token prediction under that sharpened posterior is what looks, from the outside, like “learning the task from examples.”</li> </ul> <p>No parameter update is needed for any of this — the update is Bayesian, and it happens entirely within the forward pass, over the model’s belief about which concept applies.</p> <h2 id="empirical-support-ginc">Empirical Support: GINC</h2> <p>The theory is paired with experiments on a synthetic benchmark the authors call <strong>GINC</strong> (Generative IN-Context learning dataset), built directly from an HMM mixture matching the theory’s assumptions.</p> <ul> <li>Accuracy on the synthetic ICL task increases with the number of demonstrations, and is fairly robust to demonstration ordering — both predicted directly by the Bayesian account.</li> <li>Real pretrained LMs (GPT-2, GPT-3) show qualitatively similar demonstration-count scaling on natural ICL tasks, though the authors present this as suggestive rather than as proof that real LMs satisfy the paper’s formal assumptions exactly.</li> </ul> <h2 id="takeaways">Takeaways</h2> <ol> <li>ICL doesn’t require a new learning mechanism — under the right pretraining distribution, it’s a byproduct of ordinary next-token prediction.</li> <li>The object the model infers from demonstrations is a <strong>latent concept</strong>, not a new function learned on the fly.</li> <li>This gives uncertainty a precise meaning in the ICL setting: the posterior over concepts is not concentrated on one option. That distinguishes an ICL failure caused by genuinely ambiguous demonstrations (a property of the evidence — aleatoric) from one caused by a poorly calibrated posterior even given clear evidence (a property of the model — epistemic).</li> <li>This is the theoretical anchor for treating ICL uncertainty as uncertainty over a latent variable at all, rather than only as variance in the model’s final output.</li> </ol>]]></content><author><name></name></author><category term="paper-notes"/><category term="uncertainty"/><category term="in-context-learning"/><category term="theory"/><summary type="html"><![CDATA[Xie, Raghunathan, Liang, and Ma (ICLR 2022) give a formal Bayesian account of why in-context learning improves with more demonstrations without any gradient update.]]></summary></entry><entry><title type="html">Paper notes — Function Vectors in Large Language Models</title><link href="https://smk2295.github.io/blog/2025/function-vectors/" rel="alternate" type="text/html" title="Paper notes — Function Vectors in Large Language Models"/><published>2025-07-22T01:00:00+00:00</published><updated>2025-07-22T01:00:00+00:00</updated><id>https://smk2295.github.io/blog/2025/function-vectors</id><content type="html" xml:base="https://smk2295.github.io/blog/2025/function-vectors/"><![CDATA[<p><strong>Todd, E., Li, M. L., Sharma, A. S., Mueller, A., Wallace, B. C., &amp; Bau, D. (2024). <a href="https://arxiv.org/abs/2310.15213">Function Vectors in Large Language Models</a>. ICLR 2024.</strong></p> <h2 id="the-question">The Question</h2> <p>When a language model sees a few-shot prompt like “hot → cold, big → small, fast → ___”, it produces the right answer with no gradient update at all. Somewhere in the forward pass, the model has to represent <em>which task the demonstrations are specifying</em>. Where does that representation live?</p> <h2 id="method-averaging-activations-into-a-single-vector">Method: Averaging Activations Into a Single Vector</h2> <p>The paper studies several autoregressive transformers (GPT-J, GPT-NeoX, Llama-2, and others) across a broad set of in-context tasks — lexical mappings (antonym, synonym, translation) as well as more abstract relations.</p> <p>The construction has two steps:</p> <ol> <li><strong>Locate the relevant heads.</strong> A causal-mediation analysis identifies the small set of attention heads whose activations, when altered, change the model’s task-following behavior the most.</li> <li><strong>Average across examples.</strong> For a fixed task, the activations of those heads are averaged across many different few-shot prompts of that task, at the last token position. Averaging cancels out example-specific noise and leaves one vector per task — the <strong>function vector (FV)</strong>.</li> </ol> <h2 id="the-causal-test">The Causal Test</h2> <p>The important experiment isn’t just finding a vector that correlates with a task — it’s showing the vector <em>causes</em> the behavior:</p> <ul> <li>The FV is added to the residual stream during a <strong>zero-shot</strong> forward pass — a prompt with no demonstrations at all.</li> <li>The model performs the task anyway, recovering a large fraction of true few-shot accuracy, despite never seeing a single input–output example in that forward pass.</li> </ul> <h2 id="robustness-checks">Robustness Checks</h2> <table> <thead> <tr> <th>Check</th> <th>Result reported</th> </tr> </thead> <tbody> <tr> <td>Different prompt template</td> <td>Same FV still induces the task when patched into a differently-worded prompt</td> </tr> <tr> <td>Head count</td> <td>Only a small, consistent subset of heads (tens, out of thousands) carries the effect</td> </tr> <tr> <td>Ablation</td> <td>Removing the identified heads kills ICL for that task; removing random heads of the same count does not</td> </tr> <tr> <td>Vector arithmetic</td> <td>Combining FVs for related tasks produces semantically related (though not always fully interpretable) behavior</td> </tr> </tbody> </table> <h2 id="takeaways">Takeaways</h2> <ol> <li>“Learning the task from examples” reduces to a single, low-dimensional, addable object — not a diffuse, whole-network state.</li> <li>That object lives in a sparse, identifiable set of attention heads, not spread evenly across the model.</li> <li>Because the FV is a concrete, extractable thing, it becomes a natural object to reason about uncertainty <em>over</em> — not just a lever for inducing behavior. That reframing is the starting point for treating ICL uncertainty as uncertainty about a latent task representation, rather than only about the model’s final output.</li> </ol>]]></content><author><name></name></author><category term="paper-notes"/><category term="uncertainty"/><category term="in-context-learning"/><category term="interpretability"/><summary type="html"><![CDATA[Todd, Li, Sharma, Mueller, Wallace, and Bau (ICLR 2024) show that in-context learning induces a single, portable vector that causally encodes the task.]]></summary></entry><entry><title type="html">Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra</title><link href="https://smk2295.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/" rel="alternate" type="text/html" title="Google Gemini updates: Flash 1.5, Gemma 2 and Project Astra"/><published>2024-05-14T00:00:00+00:00</published><updated>2024-05-14T00:00:00+00:00</updated><id>https://smk2295.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra</id><content type="html" xml:base="https://smk2295.github.io/blog/2024/google-gemini-updates-flash-15-gemma-2-and-project-astra/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[We’re sharing updates across our Gemini family of models and a glimpse of Project Astra, our vision for the future of AI assistants.]]></summary></entry><entry><title type="html">Displaying External Posts on Your al-folio Blog</title><link href="https://smk2295.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog/" rel="alternate" type="text/html" title="Displaying External Posts on Your al-folio Blog"/><published>2022-04-23T23:20:09+00:00</published><updated>2022-04-23T23:20:09+00:00</updated><id>https://smk2295.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog</id><content type="html" xml:base="https://smk2295.github.io/blog/2022/displaying-external-posts-on-your-al-folio-blog/"><![CDATA[]]></content><author><name></name></author></entry></feed>